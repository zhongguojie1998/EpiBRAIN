defaults:
  - default

storage_path: ./Data/mouse_trainable_data  # where to store the data embedding, now only support precomputed version
refer_genom:  ./Data/source/mm10/mm10.ml.fa # path to the reference genome
context_length: 522_240  # The length of the DNA serving as one sample. For enformer, the number is 196,608
preload_data: False
used_dataset: # the dataset to load in the training process
  - train
  - valid

preprocess:
  window_size: 32
  n_window: 16320
